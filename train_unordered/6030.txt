CloneView is an incredibly useful tool for visualizing the results from the LEVER program together with the image data. CloneView is an HTML5 application, so it runs on any device that supports a modern web browser.